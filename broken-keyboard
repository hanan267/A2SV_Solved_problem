
t = int(input())
for _ in range(t):
  s = list(input())
  workinChar = set()

  i = 0
  while i < len(s):
    if i+1 < len(s) and s[i] == s[i+1]:
      i += 2
    else:
      workinChar.add(s[i])
      i += 1
  res = ""
  for char in sorted(workinChar):
    res += char
  print(res)
  
